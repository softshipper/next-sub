overwrite: true
schema: "http://localhost:3000/api/graphql"
documents: null
generates:
  ./schema/generated/resolvers-types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
